var minhaVariavel = 'minhaVariavel';

function minhaFunc(x, y){
    return x + y;
}

// Es 2015
let num = 10;
const pi = 3.14;

var numeros = [1, 2, 3];

numeros.map(function(valor){
    return valor * 2;
});

//Typescipt
numeros.map(valor => {return valor * 2;})

class Matematica {soma(x, y){return x + y;}}

var n1: string = 'sdfsgdfg';
var n1: any = 'sdfsgdfg'; // any trsansforma em qualquer valor